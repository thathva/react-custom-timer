import React from 'react'
import { ITimerOptions } from './interfaces/ITimerOptions'

const CustomTimer = (options?: ITimerOptions) => {
  return (
    <div>
      <div>00</div>:<div>00</div>:<div>00</div>
    </div>
  )
}

export default CustomTimer
